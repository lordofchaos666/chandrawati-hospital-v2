// URL of the deployed bookings worker. Update after `./backend/deploy.sh` if
// your Cloudflare workers.dev subdomain differs from the email worker's.
window.BOOKINGS_API_URL = 'https://chandrawati-bookings.drritikchandra.workers.dev';
